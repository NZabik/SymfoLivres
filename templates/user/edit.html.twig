{% extends 'base.html.twig' %}

{% block title %}FirstProject - Modification de l'utilisateur{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="mt-4">Modification de votre profil utilisateur</h1>
		{% for message in app.flashes('warning') %}
				<div class="alert alert-warning mt-4">
					{{ message }}
				</div>
			{% endfor %}
		{{ form(form) }}
        </div>

{% endblock %}